# sbs_utils Test Range - VISUAL checks for library GUI features.
#
# Conformance is covered headless by sbs_utils/tests (test_gui_list.py,
# test_layout_grid.py, ...). This range is for *looking* at the containers in a
# running console: pick a map, see it render.
#
# SEE IT (browser):
#     python -m cosmos_dev.mission_runner test_range --map gui_list --gui --use-working-tree
# (run from missions/sbs_utils/; --use-working-tree uses this working tree, not the
#  packaged sbslib.)

import maps/gui_list.mast
import maps/gui_grid.mast
import maps/gui_table.mast
import maps/gui_containers.mast
